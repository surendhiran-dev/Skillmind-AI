
import json
import sys

def two_sum(nums, target):
    # Write your solution here
    pass
f

try:
    result = sorted(two_sum([3, 3], 6))
    print(json.dumps(result))
except Exception as e:
    print(json.dumps({"__error__": str(e)}))
